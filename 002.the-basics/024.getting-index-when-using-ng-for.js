/*
    Link: https://www.udemy.com/course/the-complete-guide-to-angular-2/learn/lecture/6655944#overview

    You can extract the index of the current iteration in the *ngFor directive using the following syntax:

    <app-server *ngFor="let server of servers; let i = index"></app-server>

    Here the = index is sort of a reserved expression in Angular under the *ngFor syntax.

 */